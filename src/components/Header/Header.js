import React from "react";
import { HeaderContainer, LogoIMG, Divider } from "./Header.styled";

const Header = () => {
  return (
    <HeaderContainer>
      <LogoIMG />
      <Divider />
    </HeaderContainer>
  );
};

export default Header;
