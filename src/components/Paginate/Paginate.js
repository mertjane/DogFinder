import React from "react";
import { Wrapper, LoadBtn } from "./Paginate.styled";

const Paginate = () => {
  return (
    <Wrapper>
      <LoadBtn>Load more... (page)</LoadBtn>
    </Wrapper>
  );
};

export default Paginate;
